# Cloud provider API keys. Copy to .env (gitignored) and fill in as needed.
# Only required when the corresponding provider is selected in config.yaml.

OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Optional: override the Ollama host (otherwise providers.ollama_host in config.yaml)
# OLLAMA_HOST=http://localhost:11434
